version: "3"
services:
  cacheregistry:
    image: registry:2
    volumes:
      - "registrystorage:/var/lib/registry/"
      - "./config.yml:/etc/docker/registry/config.yml"
    restart: always
    ports:
      - "5000:5000"
volumes:
  registrystorage: